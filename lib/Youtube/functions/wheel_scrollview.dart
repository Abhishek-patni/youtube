//Future purpose

/*
// ignore_for_file: camel_case_types

import 'dart:math';
import 'package:flutter/material.dart';
class Wheel_Scroll extends StatelessWidget {
  const Wheel_Scroll({super.key});

  @override
  Widget build(BuildContext context) {
    return ListWheelScrollView(
      useMagnifier: true,
      magnification:1,
      itemExtent: 100,
      children: listitems(context),
    );
  }
}
List<Widget> listitems (BuildContext context) {
  List <Widget> items = [
    Container(
      height: 1000,
      color: Colors.red,
      child: Center(child: Text('Mp3')),
    ),
    Container(
      height: 10,
      color: Colors.blue,
      child: Center(child: Text('mp4')),
    ),
  ];
  return items;
}
*/
